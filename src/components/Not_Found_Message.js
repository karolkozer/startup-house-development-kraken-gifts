import React from 'react';

const NotFoundMessage = () => (
	<div className="not-found-message">
		<h3 className="heading-third">
			No results found <span className="heading-third__span_icon">;(</span>
		</h3>
	</div>
);

export default NotFoundMessage;
